# Recommendation: copy to deploy/.env, then run compose from deploy/ so it auto-loads this file:
#   cd deploy
#   docker compose up -d --build

# Optional: use a prebuilt image instead of local build.
# CATNAP_IMAGE=ghcr.io/<owner>/catnap:latest

# Reverse-proxy injected header name that catnap trusts.
CATNAP_AUTH_USER_HEADER=X-User-Id

# SQLite persistence path inside the container (volume-mounted).
CATNAP_DB_URL=sqlite:///data/catnap.db

# Upstream cart page to monitor.
CATNAP_UPSTREAM_CART_URL=https://lazycats.vip/cart

# Optional: Web Push VAPID public key (base64url).
# CATNAP_WEB_PUSH_VAPID_PUBLIC_KEY=

# Optional: polling & retention defaults.
CATNAP_DEFAULT_POLL_INTERVAL_MINUTES=1
CATNAP_DEFAULT_POLL_JITTER_PCT=0.1
CATNAP_LOG_RETENTION_DAYS=7
CATNAP_LOG_RETENTION_MAX_ROWS=10000
