# 接口契约（Contracts）

本目录用于存放本计划的接口契约。契约按 `Kind` 拆分，`../PLAN.md` 的 Inventory 表是唯一入口。

本计划涉及：

- `http-apis.md`: `GET /api/ops/state`、`GET /api/ops/stream`
- `events.md`: SSE 事件名、载荷、续传/reset 语义
- `db.md`: ops 事件/任务运行/通知记录的 schema 变更与留存策略
- `config.md`: ops 相关运行参数（env vars）

